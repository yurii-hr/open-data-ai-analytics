apiVersion: v1
kind: Service
metadata:
  name: transit-web-service
  namespace: transit-analytics
spec:
  type: NodePort
  selector:
    app: transit-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30000